version: 2

jobs:
  build:
    docker:
      - image: circleci/openjdk:17.0
    steps:
      - checkout
      - run:
          name: Test Configuration
          command: echo "Testing CircleCI configuration"

workflows:
  version: 2
  build-workflow:
    jobs:
      - build
